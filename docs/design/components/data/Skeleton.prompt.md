Loading placeholder shaped like the content that is arriving — a table row, a card, a KPI. Opacity-only pulse, disabled under `prefers-reduced-motion`.

```jsx
<Skeleton lines={3} />
```

Never use a spinner.