Server-side pagination under a list; page size is 20 across the product. Chevrons point by writing direction (RTL: previous = chevron-right).

```jsx
<Pagination page={page} pageCount={7} onPageChange={setPage} totalLabel="128 הזמנות" />
```