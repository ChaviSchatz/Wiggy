Type-only brand mark — an intentional addition, because the repo contains no logo asset.

```jsx
<Wordmark subtitle="מערכת ניהול הייצור" onDark />
```

Replace this component's internals the moment a real mark is supplied; every chrome surface goes through it.