Leading chevron plus a `text-body` label — the explicit one-click way back.

```jsx
<BackLink label="חזרה ללוח הייצור" onClick={goBoard} />
```

Direction-encoding icons flip with the writing direction; thing-encoding icons never do.