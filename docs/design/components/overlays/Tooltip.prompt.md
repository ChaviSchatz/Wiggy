Deep-plum label on hover or focus. Supplementary only — never the sole source of a control's name (that is `aria-label`).

```jsx
<Tooltip label="הוספת קובץ"><IconButton icon={<Icon name="paperclip"/>} label="הוספת קובץ" /></Tooltip>
```