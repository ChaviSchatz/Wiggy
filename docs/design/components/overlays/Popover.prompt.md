Small floating panel anchored to a trigger — filters, an assignee list, a date shortcut.

```jsx
<Popover open={open} content={<AssigneePicker …/>}><IconButton icon={<Icon name="user"/>} label="אחראי" /></Popover>
```