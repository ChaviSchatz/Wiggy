The quick-view peek from a board card — identity, progress, reference thumbs, quick actions (archetype H). Inline-start edge, inset 14px, `min(430px, 100vw − 28px)`.

```jsx
<Drawer open={!!task} onClose={close} title="רבקה כהן" subtitle="WO-1041 · תפירה">…</Drawer>
```

Dismissible by scrim tap and Esc, and it always offers one click through to the full order.