Immediate on/off — template active/inactive, settings switches. The knob moves on `inset-inline-start`, so it travels correctly in RTL.

```jsx
<Toggle checked={template.active} onChange={setActive} label="תבנית פעילה" />
```