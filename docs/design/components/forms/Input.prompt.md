39px text field — `radius-xs`, `line-strong` border, plum ring on a 3px `mauve-100` halo when focused.

```jsx
<FormField label="שם הלקוחה"><Input placeholder="שם מלא" /></FormField>
```

Never block paste. Validation copy goes in `FormMessage`, next to this control.