Boolean intake fields and multi-select lists. Checked state is plum; the row stays 24px tall minimum.

```jsx
<Checkbox checked={noTop} onChange={setNoTop} label="ללא טופ" description="יוצר פריט חסר" />
```