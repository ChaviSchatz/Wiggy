Due dates and sprint dates — 39px, tabular numerals, calendar glyph at the inline end.

```jsx
<DatePicker value={dueDate} onChange={setDueDate} />
```

"Today" is the business timezone, not the browser's.