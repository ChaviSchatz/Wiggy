`surface` ground, 1px `line` border, `radius-sm`, **no shadow** — the default container for a row-level object.

```jsx
<Card interactive style={{ padding: 14 }}>…</Card>
```

A resting card never elevates. Hover changes the border to `line-strong` and translates `-1px`.