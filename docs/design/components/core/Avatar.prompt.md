Circular worker avatar with a thin light ring so it reads on white and on the dark side nav; monogram fallback in a deterministic calm colour.

```jsx
<Avatar name="שירה לוי" size="md" onClick={openAssigneePicker} />
```

**Workers only.** Clients are text — name plus order code — and never get an avatar or an initials fallback.