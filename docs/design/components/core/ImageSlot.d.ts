export interface ImageSlotProps {
  /** Monospace note describing what belongs here. */
  label?: string;
  size?: number;
  radius?: string;
}
export function ImageSlot(props: ImageSlotProps): JSX.Element;
