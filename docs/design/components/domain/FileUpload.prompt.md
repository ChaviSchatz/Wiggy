Attachment intake on the work-order hub — drag-and-drop over a `cream` ground with an explicit picker button.

```jsx
<FileUpload hint="תמונות, קבצים או הקלטה" onSelect={upload} />
```

Voice notes record on device where the browser allows it, and **always** fall back to this picker in a non-secure context.