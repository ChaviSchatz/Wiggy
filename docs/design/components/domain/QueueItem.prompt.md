One task in My Work — worker-facing, so every button is 44px and the reference photo is shown when it exists.

```jsx
<QueueItem customerName="רבקה כהן" orderCode="WO-1041" taskName="תפירה" status="in_progress" statusLabel="בעבודה" onDone={done} />
```