Order progress across work stages, and the step indicator in the New Order wizard.

```jsx
<Stepper steps={["קליטה","הכנה","תפירה","סיבים","גימור","מסירה"]} current={2} />
```

Completed nodes are `sage-600` with a `sage-300` connector behind them; the current node is plum; upcoming nodes are outlined.