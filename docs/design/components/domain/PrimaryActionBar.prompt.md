The action row for a detail page, hub or wizard footer — one plum primary, quiet secondaries, destructive pushed to the inline end.

```jsx
<PrimaryActionBar primary={<Button>סימון כנמסר</Button>} secondary={<Button variant="outline">עריכת קליטה</Button>} destructive={<Button variant="danger-soft">ביטול הזמנה</Button>} />
```