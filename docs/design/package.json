{
  "name": "wiggy-design-system",
  "version": "0.1.0",
  "private": true,
  "description": "Wiggy design system components — synced to claude.ai/design via /design-sync. Not a published package; this file exists only so the converter can resolve a package root.",
  "dependencies": {
    "react": "^18",
    "react-dom": "^18"
  }
}
